
"""
Cette classe lance l'interface graphique pour jouer contre l'ordinateur si le boolean passé est a True, si non avec un autre joueur,
On a defini pour chaque strategie un Button donc si on veut jouer contre une des stratégies on a qu'a cliquer sur le button qui correspond.
"""

from hog_gui import *

if __name__ == "__main__":
    run(True)